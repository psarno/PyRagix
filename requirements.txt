# Core
numpy==1.26.4
psutil==5.9.8

# PyTorch (CPU-only or CUDA depending on your setup)
# For CPU-only, use the "+cpu" wheels instead.
torch==2.8.0+cu126

# Sentence Transformers
sentence-transformers==3.1.1

# FAISS (CPU or GPU version)
faiss-cpu==1.8.0.post1

# PaddleOCR (and deps)
paddlepaddle==2.6.1
paddleocr==2.7.0

# PDF / Document parsing
PyMuPDF==1.24.10
pillow==10.4.0
beautifulsoup4==4.12.3
